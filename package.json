{
  "name": "eb-embed-template",
  "version": "1.0.0",
  "description":
    "This is the template for embedable EB Interactive productions",
  "main": "scripts/main.ts",
  "author": "Mikkel Jensen (EB)",
  "license": "MIT",
  "scripts": {
    "dist":
      "npm run clean && npm run lint:scripts && parcel build src/index.html --public-url ./ && echo 'Allow from all' >> dist/.htaccess",
    "clean": "rm -rf dist/ && mkdir dist",
    "prettier": "prettier src/**/*  --write --single-quote",
    "start": "parcel src/index.html -p 3000",
    "lint:scripts": "tslint -c tslint.json 'src/**/*.ts'",
    "build": "npm run dist"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.7",
    "prettier": "1.14.2",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "typescript": "^3.0.1"
  }
}
