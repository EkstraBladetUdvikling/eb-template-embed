{
  "name": "eb-embed-template",
  "version": "1.0.0",
  "description":
    "This is the template for embedable EB Interactive productions",
  "main": "scripts/main.ts",
  "author": "Mikkel Jensen (EB)",
  "license": "MIT",
  "scripts": {
    "dist":
      "npm run clean && npm run lint:scripts && parcel build src/index.html && echo 'Allow from all' >> dist/.htaccess",
    "clean": "rm -rf dist/ && mkdir dist",
    "prettier": "prettier src/**/*  --write --single-quote",
    "start": "parcel src/index.html -p 3000",
    "lint:scripts": "tslint -c tslint.json 'src/**/*.ts'",
    "build": "npm run dist"
  },
  "devDependencies": {
    "parcel-bundler": "^1.5.1",
    "postcss-cssnext": "^3.1.0",
    "prettier": "1.10.2",
    "tslint": "^5.9.1",
    "tslint-config-prettier": "^1.8.0",
    "typescript": "^2.7.2"
  }
}
